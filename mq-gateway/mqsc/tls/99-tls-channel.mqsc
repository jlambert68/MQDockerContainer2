* TLS-enabled server connection channel
DEFINE CHANNEL(DEV.TLS.SVRCONN) CHLTYPE(SVRCONN) TRPTYPE(TCP) +
  SSLCIPH('TLS_RSA_WITH_AES_256_CBC_SHA256') +
  SSLCAUTH(OPTIONAL) REPLACE

* Make sure the listener is running (optional; image usually has one)
DEFINE LISTENER(TCP.LISTENER) TRPTYPE(TCP) PORT(1414) CONTROL(QMGR) REPLACE
START LISTENER(TCP.LISTENER)

* Pick up SSL/TLS repository changes
*REFRESH SECURITY TYPE(SSL)

